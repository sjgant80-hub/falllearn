name: gate
# Three checks, because this build has three ways to ship a lie:
#   · the marker could be test-theatre        -> mutation gate on learn.mjs, must be CLEAN
#   · a lesson could be impossible to finish   -> integrity tests: every weak example must FAIL its
#                                                 own exercise, every tell must be findable
#   · the page could disagree with the marker  -> rebuild and diff; a stale page grades differently
on: [push, pull_request, workflow_dispatch]
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }

      # Named explicitly, not discovered. Test-file globbing differs between Node versions, and a
      # gate whose baseline depends on which files the runner felt like finding is a gate that can
      # go red on a green tree — which is exactly how this step first failed.
      - name: the tests actually run
        run: node --test learn.test.mjs lessons.test.mjs

      - name: witness - the tests are not theatre
        run: |
          # v0.6 or newer. Before it, a quoted --test command line was spawned as an executable
          # NAME on POSIX, so the gate never ran on ubuntu and reported a red baseline instead.
          git clone --depth 1 --branch v0.6 https://github.com/sjgant80-hub/witness.git /tmp/w
          TESTS="node --test learn.test.mjs lessons.test.mjs"
          echo "baseline, exactly as the gate will run it:"
          $TESTS || { echo "the suite is red before any mutation - fix that first"; exit 1; }
          node /tmp/w/witness.mjs mutate learn.mjs --test "$TESTS" | tee /tmp/gate.json
          node -e "
            const j=JSON.parse(require('fs').readFileSync('/tmp/gate.json','utf8').match(/\{[\s\S]*\}/)[0]);
            if(j.baselineFailed){ console.error('the gate could not run: '+j.reason); process.exit(1); }
            if(!j.killed){ console.error('NOTHING WAS TESTED - zero mutants is not a pass'); process.exit(1); }
            if(!j.clean){ console.error(j.survived.length+' mutant(s) survived - those lines are test-theatre');
              for(const s of j.survived) console.error('  '+s.line+' '+s.mutation+' | '+s.snippet); process.exit(1); }
            console.log('witness CLEAN - score '+j.score+', '+j.killed+' mutants killed');
          "

      - name: THE PAGE IS THE KERNEL - rebuild and diff
        run: |
          node build-page.mjs
          git diff --exit-code -- index.html || {
            echo "index.html is stale: the kernel changed and the page still serves the old one.";
            echo "Run: node build-page.mjs && git add index.html"; exit 1; }

      - name: the page has no dead controls
        run: |
          node -e "
            const s=require('fs').readFileSync('index.html','utf8');
            const ids=[...s.matchAll(/id=\"([A-Za-z][\w-]*)\"/g)].map(m=>m[1]);
            // A submit button is wired through its FORM, not through itself — listing it as dead
            // is how a correctness check trains people to ignore correctness checks.
            const submits=[...s.matchAll(/<button[^>]*id=\"([\w-]+)\"[^>]*type=\"submit\"|<button[^>]*type=\"submit\"[^>]*id=\"([\w-]+)\"/g)]
                            .map(m=>m[1]||m[2]);
            const wired=i=>new RegExp(\"'#\"+i+\"'\").test(s)||submits.includes(i);
            const dead=ids.filter(i=>/^(theme|answer|steps|main|out|proof)$/.test(i) && !wired(i));
            if(dead.length){ console.error('controls with no handler: '+dead.join(', ')); process.exit(1); }
            if(/__[A-Z][A-Z0-9_]*__/.test(s)){ console.error('unresolved placeholder served to a visitor'); process.exit(1); }
            console.log('page ok - '+ids.length+' ids, every named control is wired');
          "
